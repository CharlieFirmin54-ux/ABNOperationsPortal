import type { InboxEmail, Job, JobNote, NotificationItem, Property } from "@/lib/types";

export const OPERATOR = {
  name: "Charlie",
  role: "Administrator",
  email: "charlie@abnmaintenance.co.uk",
};

export const properties: Property[] = [];
export const jobs: Job[] = [];
export const emails: InboxEmail[] = [];
export const notes: JobNote[] = [];
export const notifications: NotificationItem[] = [];
